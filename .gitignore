target/
methods/guest/target/
*.bin
.chronicle_registry-state
.chronicle_registry-state.tmp
.scaffold

# batch-anchor — sibling tool living under chronicle-registry/
batch-anchor/target/
batch-anchor/store/store.sqlite3*

# Pre-built FFI .so vendored by `make ffi` — staged for chronicle's flake
# postInstall. Binary; rebuilt from sources, not committed.
logos-chronicle/vendored/*.so
result
result-*
