target/
methods/guest/target/
*.bin
.chronicle_registry-state
.chronicle_registry-state.tmp
.scaffold

# batch-anchor — sibling tool living under chronicle-registry/
batch-anchor/target/
batch-anchor/store/store.sqlite3*

# Pre-built FFI .so vendored by `make ffi`. Committed to git so cloners
# can `nix build` without first building the (heavy LEZ-dep) Rust crate.
# x86_64-linux only — see logos-chronicle/docs/anchoring.md § Distribution
# for other architectures.

result
result-*

# Project-local basecamp data dir created by scripts/run-app.sh
# (plugins, modules, module_data, logs). Treat as build output.
.basecamp-data/
